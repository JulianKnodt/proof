extern crate proof;

fn main() {
  // get input str
//  let mut buffer = String::new();
//  io::stdin().read_to_string(&mut buffer).unwrap();
//
//  // tokenize input str and then turn into ast
//  // then evaluate
//  println!("{:?}", parse(buffer).to_ast().eval(&Expr::new_env()));
}
